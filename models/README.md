# Models

Model weights and checkpoints are not included in this repository.

Recommended local structure:

```text
models/
└─ checkpoints/  # ignored by Git
```

Before sharing any pretrained or fine-tuned model weights, check the licenses of the upstream model, datasets, SDKs, and laboratory policy.
